syntax = "proto3";

package transport;
option go_package = "transport";

message Transport {
    oneof svc {
        int32 addr = 1;
        string name = 2;
    }
    int64 session = 3;
    bytes msg = 4;
}

service TransportService {
    rpc Call(Transport) returns(Transport);
}